Choosing where to insert manual prefetches
===========================================

Choice A:
=========
1. manual prefetch object this.img.inputImage[] the first 20 objects outside the computation
   and every 15th iteration prefetch the next N=30 objects
2. 
